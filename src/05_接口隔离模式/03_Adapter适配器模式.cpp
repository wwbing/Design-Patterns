#include <iostream>
/*
    动机：
        软件系统中，由于应用环境的变化，常常需要将一些现存的对象放到新的环境中使用
        但是新的环境要求的接口是这些现存对象不满足的

    适配器模式定义：
        将一个类的接口转换成客户希望的另外一个接口，使原本由于接口不兼容而不能一起工作的类可以一起工作。

	原先存在的接口不变（稳定），新的需求需要的接口也不变（稳定），采用适配器类进行老接口->新接口的转换（变化）
*/



int main()
{
    return 0;
}